# SumelaOS upstream — single source of truth for the framework repo URL.
# Used by scripts/update.sh (where to pull core updates from) and by the pull-time
# "newer version available" check (.sumela/git-hooks/_lib.sh sumela_update_check).
# First non-comment, non-blank line wins. A fork can repoint this at its own remote;
# if absent/empty, both fall back to the shipped default.
https://github.com/tolgakisaogullari/SumelaOS.git
