# SELF-IMPROVEMENT PROTOCOL — POINTER

This rule no longer holds content. The full behavioral contract for the self-improvement loop lives in:

- **Skill (workflow + safety invariants):** `.sumela/skills/self-improvement-curator/SKILL.md`
- **Schema (entry format + lifecycle):** `docs/second-brain/wiki/_SCHEMA.md` Section 15
- **Queue (active state):** `docs/second-brain/wiki/_improvement-queue/` (one `IMP-*.md` per signal)
- **Review command:** `/evolve`

If a question is "what should the agent do?", read the skill. If it is "what is the entry shape?", read the schema. Do NOT add new behavioral rules here — append them to the skill so all IDEs see them.
