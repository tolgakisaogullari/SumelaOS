# Teammate Relay — Python dependencies (runtime target: Python 3.10+)
# Pinned for reproducibility. plyer is soft-imported (see client/relay_daemon.py),
# so a missing/broken notifier never crashes the daemon.
websockets==12.0
PyNaCl==1.5.0
PyJWT==2.8.0
cryptography==42.0.8   # EdDSA session-token signing (explicit, not transitive — review I2)
plyer==2.1.0

# Dev/test only:
#   pytest==8.4.2
